# Flight Log 사용 안내

## 평소 기록하기

```text
Flight Log 페이지 열기
→ + 비행 추가
→ 날짜 / 출발 / 도착 / 항공사 / 편명 / 기종 입력
→ 비행 기록 저장
→ 필요한 비행을 계속 추가
→ 내 비행 기록 보기
```

사이트를 열거나 새로고침하면 저장 기록 수와 관계없이 항상 기록 화면에서 시작합니다. 저장된 비행은 IndexedDB에서 불러와 최신순으로 바로 표시되며, 저장 후에도 기록 화면에 머뭅니다. 추가한 비행을 확인하고 `수정`하거나, `삭제`를 한 번 눌러 즉시 지우거나, 다음 비행을 이어서 입력하세요. 준비가 되면 `내 비행 기록 보기`를 눌러 지도, 통계, 순위, 타임라인, `PLAY MY JOURNEY`로 이동합니다.

전체 아카이브에서 여러 기록을 정리하려면 상단의 `기록 관리`, 한 편만 바로 추가하려면 `+ 비행 추가`를 사용하세요. 어느 화면에서 새로고침하더라도 다음 시작 화면은 기록 관리 화면입니다.

개별 비행의 `삭제`에는 확인 대화상자가 없습니다. 한 번 누르면 즉시 삭제됩니다. 반면 `데이터 관리 → 모든 비행 기록 삭제`는 전체 데이터를 지우는 작업이므로 기존의 강한 확인 문구 입력을 계속 요구합니다.

## 꼭 기억할 점

- 기록은 **지금 사용하는 브라우저의 IndexedDB**에 남습니다.
- 계정이나 서버로 자동 전송·동기화되지 않습니다.
- 브라우저 또는 사이트 데이터를 지우면 기록이 사라질 수 있습니다.
- `데이터 관리 → JSON 백업`으로 정기적으로 백업하세요.
- 다른 기기나 브라우저에서는 JSON 백업을 가져와 `병합` 또는 `교체`하면 됩니다.

웹사이트 코드 업데이트와 내 비행 기록 업데이트는 별개입니다. 비행을 추가하는 데 Git 수정, GitHub 푸시, 사이트 재배포는 필요하지 않습니다.

기존 CSV·Excel 기록이 있다면 `데이터 관리`의 가져오기를 처음 한 번 사용할 수 있습니다. 이후에는 `+ 비행 추가`가 기본 사용 방법입니다.
