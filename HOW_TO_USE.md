# Flight Log 사용 안내

## 평소 기록하기

```text
Flight Log 페이지 열기
→ + 비행 추가
→ 날짜 / 선택적 출발 시각 / 출발 / 도착 / 항공사 / 편명 / 기종 입력
→ 비행 기록 저장
→ 필요한 비행을 계속 추가
→ 내 비행 기록 보기
```

사이트를 열면 항상 기록 0개인 기록 화면에서 시작합니다. 저장 후에는 같은 페이지 세션의 최신순 목록에 바로 나타나며, 추가한 비행을 `수정`하거나 `삭제`를 한 번 눌러 즉시 지우거나 다음 비행을 이어서 입력할 수 있습니다. 준비가 되면 `내 비행 기록 보기`를 눌러 지도, 통계, 순위, 타임라인, `PLAY MY JOURNEY`로 이동합니다.

전체 아카이브에서 여러 기록을 정리하려면 상단의 `기록 관리`, 한 편만 바로 추가하려면 `+ 비행 추가`를 사용하세요. 같은 페이지에서 두 화면을 오가는 동안에는 기록이 유지되지만, 어느 화면에서든 새로고침하면 모든 세션 기록이 사라지고 빈 기록 화면으로 돌아갑니다.

개별 비행의 `삭제`에는 확인 대화상자가 없습니다. 한 번 누르면 즉시 삭제됩니다. 반면 `데이터 관리 → 모든 비행 기록 삭제`는 전체 데이터를 지우는 작업이므로 기존의 강한 확인 문구 입력을 계속 요구합니다.

## 꼭 기억할 점

- 기록은 **현재 페이지가 열려 있는 동안만** 메모리에 유지됩니다.
- 새로고침·재방문·새 탭에서는 빈 기록으로 시작합니다.
- 계정이나 서버로 자동 전송·동기화되지 않습니다.
- 기록을 남기려면 페이지를 떠나기 전에 `데이터 관리 → JSON 백업` 또는 `CSV 내보내기`를 사용하세요.
- JSON 복원과 기존 CSV/Excel 가져오기는 현재 세션에만 반영되며, 가져온 뒤 새로고침해도 초기화됩니다.

웹사이트 코드 업데이트와 내 비행 기록 업데이트는 별개입니다. 비행을 추가하는 데 Git 수정, GitHub 푸시, 사이트 재배포는 필요하지 않습니다.

기존 CSV·Excel 기록이 있다면 `데이터 관리`의 가져오기를 처음 한 번 사용할 수 있습니다. 이후에는 `+ 비행 추가`가 기본 사용 방법입니다.

공항 입력에서는 IATA 코드와 영문 공항명·도시뿐 아니라 검토된 일부 한국어 지역 별칭도 사용할 수 있습니다. 예를 들어 `인천`은 ICN, `김포`는 GMP, `부산` 또는 `김해`는 PUS를 찾습니다. 한국어 별칭은 대한민국 주요 공항과 일부 주요 국제 허브를 우선한 제한적 목록이며, 검색어는 외부 공항·번역 서비스로 전송되지 않습니다.

## 출발 시각과 예상 도착

`출발 시각`은 선택 사항이며 출발 공항에서 보이는 현지 `HH:mm`을 입력합니다. 브라우저나 현재 위치의 시간으로 바꾸어 입력하지 마세요. 입력하지 않아도 날짜만으로 정상 저장되고 지도·통계·Timeline·재생을 모두 사용할 수 있습니다. 같은 날 여러 편이라면 시각이 있는 기록은 시각순, 시각이 없는 기록은 그 뒤의 안정된 기존 순서로 재생됩니다.

알려진 양쪽 공항의 IANA 시간대와 출발 시각이 모두 있으면 비행 재생 카드에 현지 출발, `예상 도착`, `예상 비행시간`이 나타납니다. 이는 실제 항공편 스케줄이나 실적이 아니라 대권 거리 기반의 단순 추정입니다. DST와 날짜 변경선은 해당 날짜의 공항 시간대로 계산하지만 바람·기상·ATC·실제 운항 경로는 반영하지 않습니다. 지상·해상 환승에는 이 항공편 정보가 나타나지 않으며 재생 속도도 달라지지 않습니다.

## 항공사 자동완성

항공사 칸에서 영문 이름, IATA 또는 ICAO 코드를 입력하면 로컬 목록에서 최대 8개를 제안합니다. 예를 들어 `KE`, `KAL`, `kor`, `Korean`으로 `Korean Air`를 찾을 수 있습니다. 방향키와 Enter, Escape 또는 마우스·터치로 선택할 수 있습니다. 선택하면 영문 canonical 이름, 선택과 일치하는 코드와 선택적 국가가 현재 기록에 함께 보존됩니다. 여러 코드를 가진 항공사를 코드로 찾으면 그 검색 코드가 저장되고, 같은 이름의 항공사는 코드·국가로 구분됩니다.

목록은 현재 운항사뿐 아니라 구조화된 이력으로 2000년 이후 운항이 확인되거나 그 이전 해산이 확실하지 않은 역사적 항공사를 보수적으로 포함합니다. Wikidata 이력이 완전하지 않을 수 있으며, 원하는 항공사가 없으면 입력한 자유 텍스트를 그대로 저장할 수 있습니다. 검색어는 외부 서비스로 전송되지 않습니다.

JSON V2 백업은 선택적 시각, 공항 시간대와 선택한 항공사 이름·코드·국가 메타데이터를 보존합니다. 예전 V1 JSON도 계속 복원되며 없던 값을 임의로 채우지 않습니다. CSV는 시각이 있으면 `2026.08.19 14:30`, 없으면 기존처럼 `2026.08.19`를 사용하고, 기존 CSV/XLS/XLSX의 점/대시 날짜+시각도 가능한 경우 가져옵니다. 특정 행의 현지 시각이 DST 전환으로 존재하지 않으면 그 행만 건너뛰고 나머지 유효한 행은 계속 가져옵니다.
